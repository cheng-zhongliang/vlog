/* Internal use: Only for vlog module itself. */
VLOG_MODULE(vlog)

/* Internal use: Just a couple of test modules. */
VLOG_MODULE(test_vlog1)
VLOG_MODULE(test_vlog2)

/* More modules can be added here. */

#undef VLOG_MODULE